const fs = require("fs");

const minimalWasm = new Uint8Array([
  0x00, 0x61, 0x73, 0x6d, // WASM "\0asm" header
  0x01, 0x00, 0x00, 0x00  // WASM version 1
]);

fs.writeFileSync("./public/ds.wasm", minimalWasm);
console.log("Created placeholder ds.wasm");
